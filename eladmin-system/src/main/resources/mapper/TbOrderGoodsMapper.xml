<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.zhengjie.modules.tb_order_goods.service.mapper.TbOrderGoodsMapper">

  <!-- 通用查询结果列 -->
  <!--
  <sql id="Base_Column_List">
      a.order_id, a.good_id, a.good_num, a.price, a.total_price, a.batch, a.product_date, a.valid_date, a.remark
  </sql>
  -->
  <delete id="deleteByOrderId">
    DELETE FROM tb_order_goods WHERE order_id = #{orderId}
  </delete>
</mapper>
